 <%= form_with url: search_pokemons_path, method: :get, local: true, class: "d-flex my-2", style: "width=200px" do %>
    <%= text_field_tag :query, params[:query], class: "form-control  me-2", placeholder: "Name, element or address..." %>
    <%= submit_tag "Search", class: "btn btn-warning" %>
 <% end %>
