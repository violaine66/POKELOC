<div class="container">
  <div class="text-center mt-4">

    <% if user_signed_in?%>
      <p> Hello <%= current_user.name %></p>
    <% else %>
      <p>"not connected"</p>
    <% end %>
  </div>
</div>
